# 3B 模型预训练优化器配置
# 基于 Chinchilla scaling + OLMo-2 经验值
lr: 3.0e-4               # peak learning rate
weight_decay: 0.1
betas: [0.9, 0.95]
eps: 1.0e-8
max_grad_norm: 1.0
warmup_steps: 2000        # ~0.5% of total steps
